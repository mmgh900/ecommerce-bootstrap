export default interface IProduct {
    id: string,
    namePersian: string,
    techNumber: string,
    price: number,
    isExists: boolean,
    lastBuyDate: string,
    company: string,
    car: string,
    section: string,
    isNewest: boolean,
    discount: number,
    count: number,
}